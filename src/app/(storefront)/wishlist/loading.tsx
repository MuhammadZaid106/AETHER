import { LoadingState } from "@/components/ui/LoadingState";

export default function WishlistLoading() {
  return (
    <LoadingState
      variant="fullscreen"
      theme="dark"
      text="YOUR WISHLIST"
    />
  );
}
